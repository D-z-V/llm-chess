// src/utils/helpers.ts

// Example helper function to validate and format moves (if needed)
export const formatMove = (move: string): string => {
    // Implement any custom formatting here if required
    return move;
  };
  